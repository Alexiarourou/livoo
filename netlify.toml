# Netlify config (optional). Primary hosting: Vercel — see vercel.json
[build]
  publish = "."
  # No build command - deploy HTML files as-is

[[redirects]]
  from = "/community"
  to = "/community.html"
  status = 301
